 const player = {
    currentChoice: null
 }

 const computer = {
    currentChoice: null
 }
 const choices = ["rock", "paper", "scissors"];
   
   
   
   // This code block find a random number between 0-2 which acts as the computer's choice.
    function getComputerChoice(x){
      x=3;
      let z = Math.floor(Math.random() * x);
      return 
    };

    computerChoice == [(getComputerChoice())];
console.log(computerChoice);